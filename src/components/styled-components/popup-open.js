import styled from "styled-components"

export const PopupOpen = styled.button`
  background: #121213;
  color: inherit;
  border: none;
  padding: 0;
  cursor: pointer;
  height: 30px;
  margin-right: 5px;

  img {
    height: inherit;
    width: fit-content;
  }
`
